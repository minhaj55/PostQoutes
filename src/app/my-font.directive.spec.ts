import { MyFontDirective } from './my-font.directive';

describe('MyFontDirective', () => {
  it('should create an instance', () => {
    const directive = new MyFontDirective();
    expect(directive).toBeTruthy();
  });
});
