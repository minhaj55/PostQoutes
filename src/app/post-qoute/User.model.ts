export class User {
     userName: string;
    useremail: string;
     userQuote: string;
     constructor(name, email, quote) {
         this.userName = name;
         this.useremail = email;
         this.userQuote = quote;
     }
}
